diff --git a/source/usbhsfs_manager.c b/source/usbhsfs_manager.c
index 8b21e49..b48e0bc 100644
--- a/source/usbhsfs_manager.c
+++ b/source/usbhsfs_manager.c
@@ -504,43 +504,7 @@ bool usbHsFsManagerIsDriveContextPointerValid(UsbHsFsDriveContext *drive_ctx)
 /* Non-static function not meant to be disclosed to users. */
 UsbHsFsDriveLogicalUnitContext *usbHsFsManagerGetLogicalUnitContextForFatFsDriveNumber(u8 pdrv)
 {
-    UsbHsFsDriveLogicalUnitContext *ret = NULL;
-
-    SCOPED_LOCK(&g_managerMutex)
-    {
-        if (!g_driveCount || !g_driveContexts || pdrv >= FF_VOLUMES)
-        {
-            USBHSFS_LOG_MSG("Invalid parameters!");
-            break;
-        }
-
-        for(u32 i = 0; i < g_driveCount; i++)
-        {
-            UsbHsFsDriveContext *drive_ctx = g_driveContexts[i];
-            if (!drive_ctx) continue;
-
-            for(u8 j = 0; j < drive_ctx->lun_count; j++)
-            {
-                UsbHsFsDriveLogicalUnitContext *lun_ctx = drive_ctx->lun_ctx[j];
-
-                for(u32 k = 0; k < lun_ctx->fs_count; k++)
-                {
-                    UsbHsFsDriveLogicalUnitFileSystemContext *fs_ctx = lun_ctx->fs_ctx[k];
-
-                    if (fs_ctx->fs_type == UsbHsFsDriveLogicalUnitFileSystemType_FAT && fs_ctx->fatfs && fs_ctx->fatfs->pdrv == pdrv)
-                    {
-                        ret = lun_ctx;
-                        goto end;
-                    }
-                }
-            }
-        }
-
-end:
-        if (!ret) USBHSFS_LOG_MSG("Unable to find a matching LUN context for filesystem context with FatFs drive number %u!", pdrv);
-    }
-
-    return ret;
+    return usbHsFsMountGetLogicalUnitContextForFatFsDriveNumber(pdrv);
 }
 
 /* Used to create and start a new thread with preemptive multithreading enabled without using libnx's newlib wrappers. */
diff --git a/source/usbhsfs_manager.h b/source/usbhsfs_manager.h
index de22a84..0e55b45 100644
--- a/source/usbhsfs_manager.h
+++ b/source/usbhsfs_manager.h
@@ -19,10 +19,8 @@
 /// This function is thread-safe.
 bool usbHsFsManagerIsDriveContextPointerValid(UsbHsFsDriveContext *drive_ctx);
 
-/// Locks the drive manager mutex to prevent the background thread from updating drive contexts while working with them.
-/// Then looks for a filesystem context with a FatFs object that holds a physical drive number matching the provided one. If a match is found, its parent LUN context is returned.
-/// Otherwise, this function returns NULL. The drive manager mutex is unlocked right before this function returns.
-/// This function is thread-safe.
+/// Returns the mounted LUN without reacquiring the manager lock from disk I/O.
+/// The caller must hold the drive lock, or the manager lock during mounting.
 UsbHsFsDriveLogicalUnitContext *usbHsFsManagerGetLogicalUnitContextForFatFsDriveNumber(u8 pdrv);
 
 #endif  /* __USBHSFS_MANAGER_H__ */
diff --git a/source/usbhsfs_mount.c b/source/usbhsfs_mount.c
index c42bce3..e89a195 100644
--- a/source/usbhsfs_mount.c
+++ b/source/usbhsfs_mount.c
@@ -190,7 +190,7 @@ static u32 *g_devoptabDeviceIds = NULL;
 static u32 g_devoptabDefaultDeviceId = DEVOPTAB_INVALID_ID;
 static Mutex g_devoptabDefaultDeviceMutex = 0;
 
-static bool g_fatFsVolumeTable[FF_VOLUMES] = { false };
+static UsbHsFsDriveLogicalUnitContext *g_fatFsVolumeTable[FF_VOLUMES];
 
 static const u8 g_microsoftBasicDataPartitionGuid[0x10] = { 0xA2, 0xA0, 0xD0, 0xEB, 0xE5, 0xB9, 0x33, 0x44, 0x87, 0xC0, 0x68, 0xB6, 0xB7, 0x26, 0x99, 0xC7 };   /* EBD0A0A2-B9E5-4433-87C0-68B6B72699C7. */
 static const u8 g_linuxFilesystemDataGuid[0x10] = { 0xAF, 0x3D, 0xC6, 0x0F, 0x83, 0x84, 0x72, 0x47, 0x8E, 0x79, 0x3D, 0x69, 0xD8, 0x47, 0x7D, 0xE4 };           /* 0FC63DAF-8483-4772-8E79-3D69D8477DE4. */
@@ -889,6 +912,11 @@ end:
     return ret;
 }
 
+UsbHsFsDriveLogicalUnitContext *usbHsFsMountGetLogicalUnitContextForFatFsDriveNumber(u8 pdrv)
+{
+    return pdrv < FF_VOLUMES ? g_fatFsVolumeTable[pdrv] : NULL;
+}
+
 static bool usbHsFsMountRegisterFatVolume(UsbHsFsDriveLogicalUnitFileSystemContext *fs_ctx, u8 *block, u64 block_addr)
 {
     UsbHsFsDriveLogicalUnitContext *lun_ctx = (UsbHsFsDriveLogicalUnitContext*)fs_ctx->lun_ctx;
@@ -933,6 +961,7 @@ static bool usbHsFsMountRegisterFatVolume(UsbHsFsDriveLogicalUnitFileSystemConte
     memcpy(fs_ctx->fatfs->win, block, sizeof(VolumeBootRecord));
 
     /* Try to mount FAT volume. */
+    g_fatFsVolumeTable[pdrv] = lun_ctx;
     ff_res = ff_mount(fs_ctx->fatfs, name, 1);
     if (ff_res != FR_OK)
     {
@@ -943,9 +972,6 @@ static bool usbHsFsMountRegisterFatVolume(UsbHsFsDriveLogicalUnitFileSystemConte
     /* Register devoptab device. */
     if (!usbHsFsMountRegisterDevoptabDevice(fs_ctx)) goto end;
 
-    /* Update FatFs volume slot. */
-    g_fatFsVolumeTable[pdrv] = true;
-
     /* Update return value. */
     ret = true;
 
@@ -953,7 +979,8 @@ end:
     /* Free stuff if something went wrong. */
     if (!ret && fs_ctx->fatfs)
     {
-        if (ff_res == FR_OK) ff_unmount(name);
+        ff_unmount(name);
+        g_fatFsVolumeTable[pdrv] = NULL;
         free(fs_ctx->fatfs);
         fs_ctx->fatfs = NULL;
     }
@@ -963,14 +990,14 @@ end:
 
 static void usbHsFsMountUnregisterFatVolume(char *name, UsbHsFsDriveLogicalUnitFileSystemContext *fs_ctx)
 {
-    /* Update FatFs volume slot. */
-    g_fatFsVolumeTable[fs_ctx->fatfs->pdrv] = false;
+    u8 pdrv = fs_ctx->fatfs->pdrv;
 
     /* Prepare mount name. */
     sprintf(name, "%u:", fs_ctx->fatfs->pdrv);
 
     /* Unmount FAT volume. */
     ff_unmount(name);
+    g_fatFsVolumeTable[pdrv] = NULL;
 
     /* Free FATFS object. */
     free(fs_ctx->fatfs);
diff --git a/source/usbhsfs_mount.h b/source/usbhsfs_mount.h
index 478816e..122ddc6 100644
--- a/source/usbhsfs_mount.h
+++ b/source/usbhsfs_mount.h
@@ -28,6 +28,9 @@ void usbHsFsMountDestroyLogicalUnitFileSystemContext(UsbHsFsDriveLogicalUnitFile
 /// Returns the total number of registered devoptab virtual devices.
 u32 usbHsFsMountGetDevoptabDeviceCount(void);
 
+/// Requires the drive lock for disk I/O, or the manager lock during mounting.
+UsbHsFsDriveLogicalUnitContext *usbHsFsMountGetLogicalUnitContextForFatFsDriveNumber(u8 pdrv);
+
 /// Sets the devoptab device from the provided filesystem context as the default devoptab device.
 /// Called by the chdir() function from devoptab interfaces.
 bool usbHsFsMountSetDefaultDevoptabDevice(UsbHsFsDriveLogicalUnitFileSystemContext *fs_ctx);
